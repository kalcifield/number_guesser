spring:
  jpa:
      database: POSTGRESQL
      show-sql: true
      hibernate:
        ddl-auto: create-drop
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/guesser
    username: postgres
    password: postgres
  data:
    rest:
      basePath: /api/guess
